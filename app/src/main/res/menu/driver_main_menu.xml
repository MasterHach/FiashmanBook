<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item android:title="@string/fish"
            android:id="@+id/id_fish"
            android:icon="@drawable/ic_fish"/>

        <item android:title="@string/bite"
            android:id="@+id/id_bite"
            android:icon="@drawable/ic_bite"/>

        <item android:title="@string/gear"
            android:id="@+id/id_gear"
            android:icon="@drawable/ic_gear"/>

        <item android:title="@string/history"
            android:id="@+id/id_history"
            android:icon="@drawable/ic_story"/>

    </group>

</menu>